# 股票分析项目协作规则

使用 Python 和 pytest，代码、测试和质量门禁都必须保持可复现。涉及数据分析、预测或评分时，记录假设，区分真实数据与测试夹具，不伪造数据或性能结果。

## 1. 质量工作流

修改源码前运行 `tools/run_quality.ps1 begin-unit`，完整阅读 `bug合集` 并写明验收标准；完成后按 `small -> medium -> heavy` 顺序测试。质量状态和 Bug 历史只能由质量门禁 CLI 维护。

## 2. 数据与测试

不覆盖原始数据；测试必须覆盖正常输入、边界输入、缺失数据和失败路径。时间序列不得随机打乱，外部行情请求必须可通过 mock 或离线夹具复现。

## 3. Agent 独立复核

不能把门禁退出码、测试数量或另一个 Agent 的结论当作正确性证明。对股票名称映射、指标公式、评分边界和关键结论，必须使用独立断言、可手算的小样本或独立基准进行复核，并记录局限性。
