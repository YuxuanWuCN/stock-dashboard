# config.py —— 所有可调配置集中一处
# 修改参数时只需改这个文件，无需翻主脚本

import os

# ============================================================
# 数据抓取参数
# ============================================================

# 历史数据窗口：往前推的自然日数（保证覆盖约 250 个交易日 + MA60 余量）
LOOKBACK_DAYS = 400

# 分析系统历史数据窗口（5 年 ≈ 1825 天，用于相似走势匹配）
LOOKBACK_DAYS_5Y = 1825

# 复权方式（本项目固定前复权）
ADJUST = "qfq"

# K 线周期
PERIOD = "daily"

# ============================================================
# 限流与重试
# ============================================================

# 每只标的抓取之间的间隔（秒），避免封 IP
REQUEST_INTERVAL = 1.5

# 单只抓取最长等待（秒）
REQUEST_TIMEOUT = 30

# 最大重试次数（同一只失败后最多重试次数）
MAX_RETRIES = 2

# ============================================================
# 数据校验阈值
# ============================================================

# 单只标的有效行数低于此值视为抓取失败
MIN_VALID_ROWS = 20

# ============================================================
# 均线参数
# ============================================================

MA_WINDOWS = [5, 10, 20, 60]

# ============================================================
# 路径（相对于仓库根目录）
# ============================================================

# 仓库根目录（src/ 的父目录）
ROOT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# 自选股列表
WATCHLIST_PATH = os.path.join(ROOT_DIR, "watchlist.csv")

# 数据输出目录
DATA_DIR = os.path.join(ROOT_DIR, "docs", "data")
KLINE_DIR = os.path.join(DATA_DIR, "kline")
ANALYSIS_DIR = os.path.join(DATA_DIR, "analysis")

# 摘要与元信息文件
SUMMARY_PATH = os.path.join(DATA_DIR, "summary.json")
META_PATH = os.path.join(DATA_DIR, "meta.json")

# ============================================================
# 时区
# ============================================================

TIMEZONE_NAME = "Asia/Shanghai"

# ============================================================
# 日志
# ============================================================

LOG_FORMAT = "%(asctime)s [%(levelname)s] %(message)s"
LOG_DATE_FORMAT = "%Y-%m-%d %H:%M:%S"
